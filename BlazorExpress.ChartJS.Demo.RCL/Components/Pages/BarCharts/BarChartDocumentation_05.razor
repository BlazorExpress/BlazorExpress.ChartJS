@attribute [Route(pageUrl)]

<PageHeroSection PageUrl="@pageUrl"
                 Title="@title"
                 Heading="@heading"
                 Description="@description"
                 ImageUrl="@imageUrl" />

<Demo Type="typeof(BarChart_Demo_05_Locale)" />

@code {
    private const string pageUrl = "/charts/bar-chart/locale";
    private const string title = "Blazor Bar Chart - Locale";
    private const string heading = "Blazor Bar Chart - Locale";
    private const string description = "By default, the chart is using the default locale of the platform on which it is running. In the following example, you will see the chart in the<b> German</b> locale (<b>de_DE</b>).";
    private const string imageUrl = "https://i.imgur.com/IX3bajc.png";
}
